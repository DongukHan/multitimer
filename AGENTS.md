# MultiTimer — Agent Guidelines

이 프로젝트는 **Expo SDK 52** 를 사용합니다.

- Expo SDK 52 문서: https://docs.expo.dev/versions/v52.0.0/
- expo-router 4.x (SDK 52 대응)
- react-native 0.76.x

모든 패키지 설치는 `npx expo install <package>` 를 사용하세요 (SDK 52 호환 버전이 자동 선택됨).
